import toTest from '../toTest';

if (toTest.machinelearning) {
  describe('StatModel', () => {
    describe('constructor', () => {
      it('should be constructable with default args', () => {

      });
    });
  });
}
